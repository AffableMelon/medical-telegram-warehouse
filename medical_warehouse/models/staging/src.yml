version: 2

sources:
  - name: raw
    description: Raw data from scrapes
    tables:
      - name: telegram_messages
        description: Raw telegram messages
      - name: yolo_detections
        description: Raw YOLO detection results
      - name: image_categories
        description: Explicit image categorization results linked to message_id
